spring:
  config:
    activate:
      on-profile: dev

database:
  oracle:
    databasePlatform: org.hibernate.dialect.OracleDialect
    ddlAuto: create-drop
    driverClassName: oracle.jdbc.OracleDriver
    url:
    username: ADMIN
    password: